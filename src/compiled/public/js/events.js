(function() {
  define(['jquery', 'underscore', 'backbone'], function($, _, Backbone) {
    var VEvents;
    VEvents = class VEvents extends Backbone.Events {};
    return VEvents;
  });

}).call(this);
